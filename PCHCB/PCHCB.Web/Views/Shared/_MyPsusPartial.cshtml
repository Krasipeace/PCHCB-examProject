@using PCHCB.Services.Data.Contracts;
@using PCHCB.Web.Infrastructure.Extensions;

@model MineViewModel

<div class="row">
    @if (Model.Psus.Any())
    {
        <h3>Power Supply Units</h3>
        <hr />
        @foreach (var item in Model.Psus)
        {
            <div class="col-md-4">
                <div class="card mb-3">
                    <img class="card-img-top img-thumbnail" src="@item.ImageUrl" alt="image">
                    <div class="card-body text-center">
                        <p>@item.Name</p>
                        <p>$@($"{item.Price:f2}")</p>
                        <p><a class="btn btn-outline-info" asp-area="" asp-controller="Psu" asp-action="Details" asp-route-id="@item.Id">Details</a></p>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <p class="mt-2">
            You dont have any Power Supply Units Yet! Add More to be able to view them!
            <a class="btn btn-outline-primary" asp-area="" asp-controller="Psu" asp-action="Add">Add Power Supply Unit</a>
        </p>
    }
</div>