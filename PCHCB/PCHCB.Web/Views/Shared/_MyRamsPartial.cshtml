@using PCHCB.Services.Data.Contracts;
@using PCHCB.Web.Infrastructure.Extensions;
@inject IRamService RamService
@inject IProviderService ProviderService

@model PCHCB.Web.ViewModels.Provider.MineViewModel
@{
    bool isUserProvider = await ProviderService.ProviderExistsByUserIdAsync(User.GetId()!);
    var rams = await ProviderService.AllRamsByProviderIdAsync(User.GetId()!);
}
<div class="row">
    @if (Model.Rams.Any())
    {
        <h3>Memory Sticks:</h3>
        <hr />
        @foreach (var item in Model.Rams)
        {
            <div class="col-md-4">
                <div class="card mb-3">
                    <img class="card-img-top img-thumbnail" src="@item.ImageUrl" alt="image">
                    <div class="card-body text-center">
                        <p>@item.Name</p>
                        <p>@item.Price</p>
                        <p><a class="btn btn-outline-info" asp-area="" asp-controller="Ram" asp-action="Details" asp-route-id="@item.Id">Details</a></p>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <p class="mt-2">
            You dont have any Memory Sticks Yet! Add More to be able to view them!
            <a class="btn btn-outline-primary" asp-area="" asp-controller="Ram" asp-action="Add">Add RAM</a>
        </p>
    }
</div>